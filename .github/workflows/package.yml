name: package
on:
  push:
    branches:
      - main

jobs:
  package:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: env setup
        run: | 
          sudo ls -al
          pwd
