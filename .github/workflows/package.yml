name: package
on:
  workflow_dispatch:
    environment:
      description: 'revision'
      required: true
      default: '0'
  #push:
  #  branches:
  #    - main

jobs:
  package:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: setup
        run: | 
          sudo ls -al
          pwd
          bash build.sh lsphp83 8.3.13 ${{ github.event.inputs.environment }} noble amd64
